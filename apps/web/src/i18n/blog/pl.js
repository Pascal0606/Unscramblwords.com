// Polish blog articles -- none written yet. Populated in a later session,
// once dictionary/tool work for Polish is finished and live. Deliberately
// starting empty rather than reusing the old zh.js AutoSEO-era articles
// (which were the same "Scrabble strategy" template as every other
// language and would need a full rewrite anyway, not just a translation).
export const pl = [];
